{
  "installCommand": "cd .. && pnpm install",
  "buildCommand": "cd .. && node_modules/.bin/vite build --config client/vite.config.ts",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
